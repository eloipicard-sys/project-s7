% ─────────────────────────────────────────────────────────────────────────────
%  MAIN DOCUMENT — S7-1500 Thermal Process Thesis
%  Author : Eloi Picard — ICAM 2025–2026
%  Compile : pdflatex → biber → pdflatex × 2
% ─────────────────────────────────────────────────────────────────────────────
\documentclass[12pt, a4paper, twoside]{report}

% ── Encoding & Language ──────────────────────────────────────────────────────
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[main=english, french]{babel}

% ── Font & Microtypography ────────────────────────────────────────────────────
\usepackage[protrusion=true, expansion=false]{microtype}
\usepackage{mathptmx}  % Times text+math (standard PS fonts, no MiKTeX map needed)
\usepackage{courier}   % Type 1 monospace

% ── Line Spacing & Paragraph ─────────────────────────────────────────────────
\usepackage{setspace}
\onehalfspacing
\setlength{\parskip}{6pt plus1pt minus1pt}
\setlength{\parindent}{0pt}

% ── Page Layout ──────────────────────────────────────────────────────────────
\usepackage[
  a4paper,
  top=2.5cm, bottom=2.5cm,
  inner=3.0cm, outer=2.5cm,
  headheight=15pt
]{geometry}

% ── Mathematics ──────────────────────────────────────────────────────────────
\usepackage{amsmath}
\usepackage{amssymb}

% ── Physical Units ───────────────────────────────────────────────────────────
\usepackage{siunitx}
\sisetup{per-mode=symbol, detect-all, group-separator={,}}

% ── Graphics ─────────────────────────────────────────────────────────────────
\usepackage{graphicx}
\graphicspath{{figures/}}
\usepackage{float}
\usepackage[font=small, labelfont=bf, margin=0.8cm]{caption}
\usepackage{subcaption}

% ── TikZ & PGFPlots ──────────────────────────────────────────────────────────
\usepackage{tikz}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usetikzlibrary{
  arrows.meta,
  positioning,
  shapes.geometric,
  shapes.misc,
  calc,
  fit,
  backgrounds,
  decorations.pathreplacing,
}

% ── Lists ────────────────────────────────────────────────────────────────────
\usepackage{enumitem}

% ── Tables ───────────────────────────────────────────────────────────────────
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{longtable}
\usepackage{array}
\renewcommand{\arraystretch}{1.3}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}

% ── Colors ───────────────────────────────────────────────────────────────────
\usepackage{xcolor}
\definecolor{darkblue}{RGB}{0,51,102}
\definecolor{icamblue}{RGB}{0,83,155}
\definecolor{codebg}{RGB}{250,250,250}
\definecolor{codeframe}{RGB}{210,210,210}
\definecolor{kwblue}{RGB}{0,0,180}
\definecolor{strred}{RGB}{163,21,21}
\definecolor{cmtgreen}{RGB}{0,128,0}
\definecolor{numgray}{RGB}{130,130,130}
\definecolor{pendingbg}{RGB}{255,245,200}
\definecolor{pendingedge}{RGB}{200,150,0}

% ── Code Listings ────────────────────────────────────────────────────────────
\usepackage{listings}

\lstdefinestyle{python}{
  language         = Python,
  backgroundcolor  = \color{codebg},
  basicstyle       = \ttfamily\small,
  keywordstyle     = \color{kwblue}\bfseries,
  stringstyle      = \color{strred},
  commentstyle     = \color{cmtgreen}\itshape,
  numberstyle      = \tiny\color{numgray},
  numbers          = left,
  numbersep        = 8pt,
  stepnumber       = 1,
  frame            = single,
  framesep         = 3pt,
  rulecolor        = \color{codeframe},
  breaklines       = true,
  tabsize          = 4,
  showstringspaces = false,
  captionpos       = b,
  xleftmargin      = 18pt,
  framexleftmargin = 14pt,
  aboveskip        = 8pt,
  belowskip        = 8pt,
  morekeywords     = {self, True, False, None, Optional, float, int, str, dict, list},
}

\lstdefinestyle{json}{
  basicstyle       = \ttfamily\small,
  backgroundcolor  = \color{codebg},
  frame            = single,
  rulecolor        = \color{codeframe},
  string           = [s]{"}{"},
  stringstyle      = \color{strred},
  comment          = [l]{//},
  commentstyle     = \color{cmtgreen}\itshape,
  numbers          = none,
  showstringspaces = false,
  breaklines       = true,
  aboveskip        = 8pt,
  belowskip        = 8pt,
}

\lstdefinestyle{yaml}{
  basicstyle       = \ttfamily\small,
  backgroundcolor  = \color{codebg},
  frame            = single,
  rulecolor        = \color{codeframe},
  comment          = [l]{\#},
  commentstyle     = \color{cmtgreen}\itshape,
  numbers          = none,
  string           = [b]",
  stringstyle      = \color{strred},
  showstringspaces = false,
  breaklines       = true,
  aboveskip        = 8pt,
  belowskip        = 8pt,
}

\lstset{style=python}   % default style

% ── Headers & Footers ────────────────────────────────────────────────────────
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\bfseries\thepage}
\fancyhead[LO]{\small\itshape Eloi Picard}
\fancyhead[RE]{\small\itshape\nouppercase{\leftmark}}
\fancyhead[RO]{\small\bfseries\thepage}
\fancyfoot[LE,RO]{\small\itshape Academic Year 2025\,--\,2026}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[C]{\small\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% ── Chapter / Section Style ───────────────────────────────────────────────────
\usepackage[explicit]{titlesec}

\titleformat{\chapter}[hang]{%
  \normalfont\Large\bfseries\color{darkblue}%
}{\thechapter}{14pt}{%
  #1%
}[{\vspace{2pt}\color{darkblue}\rule{\linewidth}{0.8pt}}]
\titlespacing*{\chapter}{0pt}{30pt}{20pt}

\titleformat{\section}[hang]{%
  \normalfont\large\bfseries%
}{\thesection}{1em}{#1}
\titlespacing*{\section}{0pt}{2ex plus .2ex minus .2ex}{1.3ex plus .2ex}

\titleformat{\subsection}[hang]{%
  \normalfont\normalsize\bfseries%
}{\thesubsection}{1em}{#1}
\titlespacing*{\subsection}{0pt}{1.5ex plus .2ex minus .2ex}{1.0ex plus .2ex}

\titleformat{\subsubsection}[hang]{%
  \normalfont\normalsize\bfseries%
}{\thesubsubsection}{1em}{#1}

% ── Bibliography ─────────────────────────────────────────────────────────────
\usepackage[
  backend    = biber,
  style      = numeric-comp,
  sorting    = none,
  maxbibnames= 6,
]{biblatex}
\addbibresource{references.bib}

% ── Hyperref (always last) ────────────────────────────────────────────────────
\usepackage[
  colorlinks  = true,
  linkcolor   = darkblue,
  citecolor   = darkblue,
  urlcolor    = icamblue,
  pdftitle    = {Supervision and Cascade Control of a Thermal Process on S7-1500},
  pdfauthor   = {Eloi Picard — ICAM 2025-2026},
  pdfsubject  = {Engineering Thesis},
  pdfkeywords = {PLC, S7-1500, cascade control, Flask, Docker, WinCC Unified},
]{hyperref}

% ─────────────────────────────────────────────────────────────────────────────
%  CUSTOM COMMANDS
% ─────────────────────────────────────────────────────────────────────────────
% Inline code / tag name (underscores escaped by user)
\newcommand{\code}[1]{\texttt{#1}}

% PLC address  e.g. \plcaddr{MD16}  →  %MD16
\newcommand{\plcaddr}[1]{\texttt{\%#1}}

% Pending notice box
\newcommand{\pendingbox}[1]{%
  \vspace{6pt}%
  \noindent\fcolorbox{pendingedge}{pendingbg}{%
    \begin{minipage}{\dimexpr\linewidth-2\fboxsep-2\fboxrule}
      \small\textbf{\color{pendingedge}Pending experimental data:} #1
    \end{minipage}%
  }\vspace{6pt}%
}

% ─────────────────────────────────────────────────────────────────────────────
%  DOCUMENT
% ─────────────────────────────────────────────────────────────────────────────
\begin{document}

%── Title page ──────────────────────────────────────────────────────────────
\input{chapters/titlepage}

%── Front matter  (roman numerals) ──────────────────────────────────────────
\pagenumbering{roman}
\setcounter{page}{2}

\input{chapters/abstract}
\input{chapters/acknowledgements}

\clearpage
\tableofcontents

\clearpage
\listoffigures
\addcontentsline{toc}{chapter}{\listfigurename}
\listoftables
\addcontentsline{toc}{chapter}{\listtablename}

\input{chapters/abbreviations}

%── Main matter  (arabic numerals) ──────────────────────────────────────────
\clearpage
\pagenumbering{arabic}
\setcounter{page}{1}

\input{chapters/ch01_introduction}
\input{chapters/ch02_process}
\input{chapters/ch03_plc}
\input{chapters/ch04_software}
\input{chapters/ch05_identification}
\input{chapters/ch06_cascade}
\input{chapters/ch07_interface}
\input{chapters/ch08_conclusion}

%── References ──────────────────────────────────────────────────────────────
\clearpage
\printbibliography[heading=bibintoc, title={References}]

%── Appendices ──────────────────────────────────────────────────────────────
\clearpage
\appendix
\input{chapters/appendices}

\end{document}
